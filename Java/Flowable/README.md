# Flowable

> Flowable 是一个用 Java 编写的轻量级业务流程引擎



## Fundamental

> 参考基本的请假流程案例: [flowable-holidayrequest](./flowable-holidayrequest/src/main/java/org/flowable)
>
> > 参考文件: [holiday-request.bpmn20.xml](./flowable-holidayrequest/src/main/resources/holiday-request.bpmn20.xml)
> >
> > BPMN Flowable-Diagram
>
> ```mermaid
> graph LR
> A(("Start"))
> B["Approve or reject request"]
> C{"X"}
> D["Send out rejection email"]
> E["Enter holidays in external system"]
> F["Holiday approved"]
> G["Reject End"]
> H["Approved End"]
> A-->B-->C-->E-->F-->H
> C-->D-->G
> ```
>



### BPMN

> Business Process Modeling Notation, 即业务流程建模符号
>
> **BPMN2.0定义了三类基本要素：Events（事件）、Gateways（网关）、Activities（活动）**

#### 事件

> 件用来表明流程的生命周期中发生了什么事, 事件总是画成一个圆圈

![](./Assets/1.png)

##### 1. 开始事件

> 开始事件用来指明流程从哪里开始
>
> 开始事件启动一个流程的新实例, 它只有一个唯一的无条件输出顺序流, 没有输入连线

![](./Assets/2.png)

##### 2. 时间开始事件

> 时间(定时)开始事件用来在指定的时间启动一个流程, 也可以在指定周期内循环启动多次流程

![](./Assets/3.png)

##### 3. 信号开始事件

> 当接收到特定的信号后信号开始事件被触发, 启动一个流程实例(可以触发多个流程)

![](./Assets/4.png)

##### 4. 消息开始事件

> 当接受到特定的消息后消息开始事件被触发, 启动一个流程实例(每个消息只允许有一个订阅者)

![](./Assets/5.png)

##### 5. 结束事件

> 结束事件表示流程或分支的**自然结束**，什么都不做

![](./Assets/6.png)

##### 6. 终止事件

> 表示流程被强制终止, 什么都不做
>
> 当流程有多个分支路线被激活时, 这些**分支上的活动任务也被终止**

![](./Assets/7.png)

##### 7. 信号结束事件

> 在信号事件结束之前, 引擎向系统内部发出一个信号，随即结束该事件，所在流程分支结束

![](./Assets/8.png)

##### 8. 消息结束事件

> 在消息事件结束之前, 引擎向系统内部(又叫内部消息)发出一条消息或者调用外部系统接口, 随即事件结束，所在流程分支结束
>
> 如果当前分支是最后一个活动分支, 流程实例结束

![](./Assets/9.png)

##### 9. 错误结束事件

> 当执行到达错误结束事件时抛出 BPMNError 业务异常, 表示流程或分支在发生业务错误后结束
>
> 如果抛出的异常未被捕获, 该流程不会结束

![](./Assets/10.png)

##### 10. 补偿结束事件

> 当执行到达补偿结束事件时触发该流程已完成任务的边界补偿事, 表示流程或分支在发生业务补偿后结束
>
> 如果执行业务补偿时抛出的异常, 该流程不会结束

![](./Assets/11.png)

##### 11. 捕获时间事件

> 在开始事件和结束事件之间发生的事件都称为中间事件
>
> 当执行到达捕获时间事件时中断在这里, 引擎会创建一个定时器, 当定时器触发后事件结束, 流程沿后继路线继续执行

![](./Assets/12.png)

##### 12. 捕获信号事件

> 当执行到达捕获信号事件时中断在这里, 等待抛出信号事件或者API发出匹配的信号后结束, 流程沿后继路线继续执行

![](./Assets/13.png)

##### 13. 捕获消息事件

> 当执行到达捕获消息事件时中断在这里, 等待抛出消息事件或者API发出匹配的消息后结束, 流程沿后继路线继续执行

![](./Assets/14.png)

##### 14. 抛出信号事件

> 当执行到达抛出信号事件时, 引擎向系统内部发出一个信号, 信号发出后事件结束, 流程沿后继路线继续执行
>
> 抛出的信号可以被信号开始事件、中间捕获信号事件、边界信号事件订阅处理

![](./Assets/15.png)

##### 15. 抛出消息事件

> 当执行到达抛出消息事件时, 引擎向系统内部(又叫内部消息)发出一条消息或者调用外部系统接口, 随即该事件结束，流程沿后继路线继续执行

![](./Assets/16.png)

##### 16. 抛出补偿事件

> 当执行到达抛出补偿事件时触发该流程已完成任务的边界补偿事件, 完成补偿操作后自动执行后继路线

![](./Assets/17.png)

##### 17. 边界时间事件

> 当执行到达边界时间事件依附的任务时(如人工任务、系统任务、子流程), 引擎会创建一个定时器, 当定时器触发后, 流程沿边界时间事件的后继路线继续执行
>
> 如果该边界事件设置为中断, 依附的任务将中断执行

![](./Assets/18.png)

##### 18. 边界信号事件

> 当执行到达边界信号事件依附的任务时(如人工任务、系统任务、子流程), 引擎会创建一个捕获事件, 在其依附的任务的生命周期内等待一个抛出信号, 该信号来自信号抛出事件或者API, 被触发后后继路线继续执行
>
> 如果该边界事件设置为中断, 依附的任务将中断执行

![](./Assets/19.png)

##### 19. 边界消息事件

> 当执行到达边界消息事件依附的任务时(如人工任务、子流程), 引擎会创建一个捕获事件, 在其依附的任务的生命周期内等待一个抛出消息, 该消息来自消息抛出事件或者API, 被触发后后继路线继续执行
>
> 如果该边界事件设置为中断, 依附的任务将中断执行

![](./Assets/20.png)

##### 20. 边界补偿事件

> 当边界补偿事件依附的节点触发回退操作时, 边界补偿事件自动被激活, 执行补偿事件后继路线上的任务

![](./Assets/21.png)

##### 21. 边界错误事件

> 当边界错误事件依附的节点任务抛出 BPMNError 业务异常时中断该任务, 边界错误事件自动被激活, 执行错误事件后继路线上的任务

![](./Assets/22.png)

##### 











